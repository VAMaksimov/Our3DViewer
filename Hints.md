| Symbol | Description |
|--------|-------------|
| `using CheckFunction = bool (WireframeObject::*)(std::istringstream &);` | `using` is a C++ keyword that allows developer to create an alias for a type. 1. `using CheckFunction` - This creates a new type name called `CheckFunction` 2. `=` - Assigns the type definition |